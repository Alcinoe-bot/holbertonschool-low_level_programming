#include "main.h"
#include "2-strlen.c"
#include "_putchar.c"
/**
* rev_string - reverse
* @s: string
* Description (): print un mot en reverse
* Return:valeur 0
*/
void rev_string(char *s)
{
int i;
for (i = _strlen(s); i >= 0; i--)
{
_putchar(*(s + i));
}
_putchar('\n');
}
