#include "main.h"
#include "2-strlen.c"
/**
* _strncpy - check the code for
* @s1: pointeur
* @s2: pointeur
* Description: concataien
* Return: 0.
*/
int _strcmp(char *s1, char *s2)
{
        int i;

        for (i = 0; i < n && src[i] != '\0'; i++)
        {
                dest[i] = src[i];
        }
        for (; i < n; i++)
        {
                dest[i] = '\0';
        }

        return (dest);
}
