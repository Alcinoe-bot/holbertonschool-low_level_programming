#include <stdio.h>
#include <ctype.h>
/**
* main - lettre minuscule
* Description: affiche les lettres minucules de la table ASCII
* Return:valeur 0
*/
int main(void)
{
int c = 0;
while (c < 17)
{
putchar(c);
c++;
}
putchar('\n');
return (0);
}
